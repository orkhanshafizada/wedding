<div class="col-12 col-md-12 text-end">
    <a href="{{ $action_cancel }}" class="btn btn-light me-2">{{ __('Cancel') }}</a>
    <button type="submit" class="btn btn-primary btn-stable" data-loading-text="{{ __('Saving') }}">
        {{ __('Save') }}
    </button>
</div>
